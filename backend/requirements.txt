# All dependencies. requests is pre-installed on most systems.
# Install with: pip install -r requirements.txt
requests>=2.28.0

# Optional: for a production ASGI server instead of the built-in server
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0
# httpx>=0.27.0
# pydantic>=2.7.0
